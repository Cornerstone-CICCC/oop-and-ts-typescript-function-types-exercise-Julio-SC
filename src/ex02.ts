// Create an anonymous function (a function without a name).
// This function should take one parameter of type string.
// It should log the string to the console. Specify the correct return type for the function.

const logMessage = function (message: string): void {
    console.log(message);
  };
  
  // Expected output:
  logMessage("Hello, World!"); // "Hello, World!"